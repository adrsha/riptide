[package]
name = "riptide"
version = "0.1.0"
edition = "2024"

[dependencies]

rt_client = { version = "0.1.0", path = "../rt_client" }
rt_core = { version = "0.1.0", path = "../rt_core" }
rt_server = { version = "0.1.0", path = "../rt_server" }
